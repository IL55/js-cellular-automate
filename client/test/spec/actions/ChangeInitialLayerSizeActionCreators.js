'use strict';

describe('ChangeInitialLayerSizeActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/ChangeInitialLayerSizeActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
