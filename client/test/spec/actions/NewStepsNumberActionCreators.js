'use strict';

describe('NewStepsNumberActionCreators', () => {
  let action;

  beforeEach(() => {
    action = require('actions/NewStepsNumberActionCreators.js');
  });

  it('should be defined', () => {
    expect(action).toBeDefined();
  });
});
