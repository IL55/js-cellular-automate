'use strict';

describe('RulesStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/RulesStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
