'use strict';

describe('RulesStoreStore', () => {
  let store;

  beforeEach(() => {
    store = require('stores/RulesStoreStore.js');
  });

  it('should be defined', () => {
    expect(store).toBeDefined();
  });
});
